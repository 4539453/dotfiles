#!/usr/bin/env bash

set -eu

if [ "$(xkb-switch -p)" = 'us' ]; then
  notify-send '🌍 EN' -t 1500 -h string:x-canonical-private-synchronous:kbd-layout
fi
if [ "$(xkb-switch -p)" = 'ru' ]; then
  notify-send '🚀 RU' -t 1500 -h string:x-canonical-private-synchronous:kbd-layout
fi
